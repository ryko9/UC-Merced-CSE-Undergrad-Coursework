comebineString pseudo

combineStr(string, numLines)
{




for(int i = 0; i < numLines; i++)

	result += string;

	return result;
}

main()
{

	while(numLines!= 0)
{
	output: Enter a string:
		input < somestring
	output: Enter a number of lines
		input < numLines

	combineStr(string, numLines)

}

}